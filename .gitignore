# OS / editor junk
.DS_Store
*.swp
*.swo
*~

# Backups created by the installer helpers if run in-tree
*.backup.*

# This machine's font sizes. fonts.local.example is the copy that IS committed.
fonts.local
