# OS / editor junk
.DS_Store
*.swp
*.swo
*~

# Backups created by the installer helpers if run in-tree
*.backup.*

# The files you edit. The .example copies beside them are the ones committed.
fonts.local
settings.local
aliases.local
